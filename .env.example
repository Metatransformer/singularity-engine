# ── Manual (you provide these) ──────────────────────────────────────────────

# ── Code Builder Auth (pick one — OAuth is prioritized over API key) ─────────
# The Anthropic SDK checks ANTHROPIC_AUTH_TOKEN first (OAuth), then ANTHROPIC_API_KEY.
# Code builder uses this to generate apps via Claude Sonnet.
#
# Option 1: OAuth session token (prioritized by SDK when both are set)
# ANTHROPIC_AUTH_TOKEN=your_oauth_session_token
#
# Option 2: Standard API key
ANTHROPIC_API_KEY=your_anthropic_api_key
#
# ── Anti-Recon TOS Layer ────────────────────────────────────────────────────
# ANTHROPIC_API_KEY is ALSO used by the TOS safety classifier (Claude Haiku)
# to catch nuanced prompt injection / policy violations that regex misses.
# Without it: code builder still works (if OAuth is set), but LLM TOS screening is skipped.

X_BEARER_TOKEN=your_x_bearer_token

# Reply Mode: "openclaw" (default, browser automation) or "x-api" (X API v2 direct posting)
REPLY_MODE=openclaw

# X API OAuth 1.0a (only needed if REPLY_MODE=x-api)
# X_CONSUMER_KEY=
# X_CONSUMER_SECRET=
# X_ACCESS_TOKEN=
# X_ACCESS_TOKEN_SECRET=

# ── Auto-detected by `singularityengine config` ────────────────────────────
# AWS_REGION        → from `aws configure get region`
# TABLE_NAME        → "singularity-db" (default)
# GITHUB_TOKEN      → from `gh auth token`
# GITHUB_REPO       → detected from singularity-builds fork
# GITHUB_PAGES_URL  → derived from GITHUB_REPO
# OWNER_USERNAME    → from X API /2/users/me or manual entry

# ── Set by other commands ───────────────────────────────────────────────────
# SINGULARITY_DB_URL   → created by `singularityengine deploy`
# WATCHED_TWEET_ID     → set by `singularityengine watch <tweet_id>`
# OPENCLAW_CDP_PORT    → 18800 (default, only used in openclaw mode)
